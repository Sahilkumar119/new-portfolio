; @vercel/speed-insights and @vercel/analytics declare an optional React 18/19
; peer; this project is on React 16 and uses their framework-agnostic inject API,
; so relax peer resolution to let installs succeed (local and on Vercel).
legacy-peer-deps=true
